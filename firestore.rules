rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Default to rejecting all requests until specific schemas are implemented
      allow read, write: if false;
    }
  }
}
